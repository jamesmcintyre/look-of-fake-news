import React from 'react';

export default (props) => {

  const { landingDimensions } = props;

  // const style = {
  //   height,
  //   width
  // };

  return (
    <div
      className={'landing-container'}
      style={landingDimensions}
      >
    </div>
  )
}
